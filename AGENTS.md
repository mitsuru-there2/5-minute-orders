# 開発ルール

- コードを修正したら、関連する `docs/specs/` のSPECも同期して更新する。
- このプロジェクトでは postgres MCP を利用しない。
- 初期開発はローカル動作のみ。DBは端末内のローカルDBのみを使用し、外部サーバー・クラウドDB・ログイン・通信接続を実行時の必須条件にしない。
- 将来のオンラインマルチプレイに備え、ゲームルールを保存・通信から分離する。オンライン機能自体は初期開発の対象外。
- 原案は `docs/CONCEPT.md`。合意済み仕様、実装済み仕様、提案を明確に区別する。
- Godotの版は `.godot-version` に固定する。GDScriptは型注釈を使う。
- domainはNode、UI、ネットワーク、ファイル、実時間に依存しない。
- ゲームロジック変更時はheadlessテストを追加し `./scripts/check.sh` を実行する。
- 秘匿状態をUIや将来の対戦クライアントに渡さない。AIにも観測可能な情報だけを渡す。
- 秘密情報、署名鍵、Godotキャッシュ、実行バイナリはコミットしない。
- 当面はmainブランチのみで開発する。作業ブランチやPRは作成せず、mainへ直接コミット・pushする。
- コミットは小さく、変更理由を明記。第三者素材の出典・ライセンスを記録する。

- GDScript変更時は `make format` で整形し、`make check` でformat・lint・Godot検証を通す。
- 開発依存は `pyproject.toml` と `uv.lock` で管理し、通常の導入は `uv sync --locked` を使う。アドオンは `docs/DEPENDENCIES.md` に固定版・取得元・ライセンスを記録する。
